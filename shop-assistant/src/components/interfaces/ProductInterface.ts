interface Product {
    name: String,
    market: String,
    priceNew: Number,
    priceOld: Number,
    difference: String,
    type: Number
    diffColor: String,
    isChecked: Boolean,
}

const products: Product[] = [];
